# Read Empatica Data

Reads Empatica device data from a specified directory.

## Usage

``` r
read.empatica(path)
```

## Arguments

- path:

  Directory path containing Empatica CSV files.

## Value

A list of data frames for different signals (ACC, BVP, EDA, TEMP, IBI).
